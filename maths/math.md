# Generate Random Whole Numbers with JavaScript

1. random and floor use

``` function randomWholeNum() {
  return Math.floor(Math.random() * 10)
}

console.log(randomWholeNum()) ```




# ParseInt in javascript

1. parseInt use with exam.

``` function convertToInteger(str) {
return parseInt(str)
}
const str = parseInt(56) ```

1. console.log(convertToInteger(""))   // Nan 
  console.log(convertToInteger("16"))  // 16 
   console.log(convertToInteger("Shivendra"))  // NAN